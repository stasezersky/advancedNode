here are examples of spawn method of child_process module

the main purpose of spawn method is to start a new child process that runs linux shell commands 

#### it doesn't starts a shell to execute the command, and it streams all the data instead of buffering it and than sending